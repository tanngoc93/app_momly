<div class="text-center">
  <p class="fw-semibold mb-3">Your QR code is ready to use.</p>
  <img src="<%= qr_code_path(url: url) %>" alt="QR Code" class="mb-3" width="300" height="300">
  <%= link_to "Download QR", qr_code_path(url: url, download: 1), class: "btn btn-primary", target: "_blank" %>
</div>
