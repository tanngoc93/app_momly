<div class="text-center d-flex flex-column align-items-center">
  <p class="fw-semibold mb-3 text-dark">Your QR code is ready to use.</p>
  <img src="<%= qr_code_path(url: url) %>" alt="QR Code" class="mb-3 qr-code-preview">
  <%= link_to "Download", qr_code_path(url: url, download: 1),
              class: "btn btn-lg w-100 btn-qr-download",
              target: "_blank",
              rel: "noopener",
              data: { turbo: false } %>
</div>
