<!DOCTYPE html>
<html>
  <head>
    <title>Momly — Clean & Powerful Link Shortener</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Favicon -->
    <link rel="icon" href="/favicon.png" type="image/x-icon">

    <!-- Stylesheets -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- JavaScript -->
    <%= javascript_importmap_tags %>
  </head>

  <body style="background-color: #00193A; font-family: 'Helvetica Neue', sans-serif;">
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark py-3" style="background-color: #00193A;">
      <div class="container-lg" style="max-width: 1140px;">
        <a class="navbar-brand fw-bold text-white" href="/">Momly</a>

        <div class="ms-auto d-flex align-items-center gap-2">
          <% if user_signed_in? %>
            <%= button_tag "My Links", class: "btn btn-outline-light btn-sm", data: { bs_toggle: "modal", bs_target: "#myLinksModal" } %>
            <%= link_to "Setting", edit_user_registration_path, class: "btn btn-outline-light btn-sm" %>
            <%= button_to "Logout", destroy_user_session_path, method: :delete,
                  class: "btn btn-light btn-sm", form: { class: "d-inline" } %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: nav_button_class(:login) %>
            <%= link_to "Sign Up", new_user_registration_path, class: nav_button_class(:signup) %>
          <% end %>
        </div>
      </div>
    </nav>

    <!-- Page content -->
    <main class="<%= content_for?(:auth_layout) ? 'flex-grow-1 d-flex flex-column justify-content-center align-items-center py-5' : '' %>">
      <%= yield %>
    </main>

    <% if user_signed_in? %>
      <div class="modal fade" id="myLinksModal" tabindex="-1" aria-labelledby="myLinksModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-scrollable">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title fw-bold" id="myLinksModalLabel">📁 My Short Links</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
              <turbo-frame id="my_links_frame" src="<%= my_links_modal_path %>" data-src="<%= my_links_modal_path %>">
                <div class="text-center py-4 text-muted">Loading your links...</div>
              </turbo-frame>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <script>
      function copyToClipboard(text) {
        navigator.clipboard.writeText(text).then(() => {
          alert("Copied to clipboard!");
        });
      }

      document.addEventListener("DOMContentLoaded", () => {
        const modalEl = document.getElementById("myLinksModal");
        if (!modalEl) return;

        modalEl.addEventListener("show.bs.modal", () => {
          const frame = document.getElementById("my_links_frame");
          if (frame) {
            frame.src = frame.dataset.src; // force reload every open
          }
        });
      });
    </script>
  </body>
</html>
