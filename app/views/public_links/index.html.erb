<% content_for :page_title, "Public Links Directory — Momly" %>
<% content_for :meta_description, "Browse every public link shared on Momly. Discover trending pages, inspiring reads, and useful resources updated in real time." %>

<div class="container py-5" style="max-width: 960px;">
  <div class="text-center text-light mb-4">
    <h1 class="fw-bold mb-1">🔥 Public Links Directory</h1>
    <p class="small mb-2">
      A curated stream of every public link shared on Momly — updated in real time with
      titles, summaries, and short links ready to explore.
    </p>
    <% if @pagy %>
      <p class="small text-light-emphasis mb-0 public-links-count">
        Showing <strong><%= @pagy.vars[:items] %></strong> per page ·
        <strong><%= @pagy.count %></strong> public links total
      </p>
    <% end %>
  </div>

  <% if @short_links.any? %>
    <ul class="list-group">
      <%= render partial: "public_link_item", collection: @short_links, as: :link %>
    </ul>
    <div class="d-flex justify-content-center mt-4">
      <%== pagy_bootstrap_nav(@pagy) %>
    </div>
  <% else %>
    <div class="text-center text-light py-5">No public links found.</div>
  <% end %>
</div>
